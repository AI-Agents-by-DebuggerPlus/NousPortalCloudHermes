<Window x:Class="AhccDesktopViewer.LogWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="AHCCDV Logs"
        Height="480"
        Width="860"
        Background="#0B1F2A"
        WindowStartupLocation="CenterOwner">
    <DockPanel Margin="12">
        <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Margin="0,0,0,8">
            <Button Content="Copy all" Width="100" Height="30" Margin="0,0,8,0" Click="CopyAll_Click" />
            <Button Content="Open log folder" Width="130" Height="30" Margin="0,0,8,0" Click="OpenFolder_Click" />
            <Button Content="Clear view" Width="100" Height="30" Click="ClearView_Click" />
            <TextBlock x:Name="PathText" Foreground="#8AA8A6" VerticalAlignment="Center" Margin="12,0,0,0"
                       TextWrapping="Wrap" />
        </StackPanel>
        <TextBox x:Name="LogBox"
                 IsReadOnly="True"
                 AcceptsReturn="True"
                 VerticalScrollBarVisibility="Auto"
                 HorizontalScrollBarVisibility="Auto"
                 FontFamily="Consolas"
                 FontSize="12"
                 Background="#123D45"
                 Foreground="#E8F4F3"
                 BorderThickness="0"
                 TextWrapping="NoWrap" />
    </DockPanel>
</Window>
